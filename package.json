{
  "name": "revealjs-flow",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "npx serve -l 8000 .",
    "build": "rm -rf public && mkdir -p public/node_modules/reveal.js && cp index.html theme.css public/ && cp -r assets public/ && cp -r node_modules/reveal.js/dist public/node_modules/reveal.js/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "reveal.js": "^6.0.1"
  }
}
